# Build
target/
!.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# Environment & secrets (AGENTS.md §4.2 — never commit)
.env
*.pem
secrets/

# OS
.DS_Store
Thumbs.db

# Runtime artifacts
docker/runtime/
perf/results/
.localstack/
# deploy.sh local state (nginx.conf in deploy/runtime stays TRACKED — it is a commit artifact)
deploy/runtime/last-deploy.txt
deploy/runtime/deploy-overlay.yaml

# Backup dumps + manifests (E14 S3 — never commit data)
backups/

# Logs
*.log

# Conversation debris / internal notes (never committed)
uploads/
internal-notes/
**/*:Zone.Identifier

# python bytecode (local tooling artifacts)
__pycache__/
